# z88dk / appmake build outputs
syncdev
syncdev_*.bin
*.map
*.o
*.lis
zcc_opt.def

# uart.asm and zproject.lst are hand-written BUILD SOURCES, not outputs. The
# parent nextsync/.gitignore's *.asm / *.lst rules (meant for SDCC-generated
# files) would otherwise drop them and make the dotN unbuildable from a clone.
!uart.asm
!zproject.lst
