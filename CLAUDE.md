# CLAUDE.md

This file provides guidance to Claude Code (claude.ai/code) when working with code in this repository.

## What this project is

ZX-Next-Unite is a cross-platform (Windows/Linux/macOS) PySide6 (Qt6) GUI application written in Python. It combines two tools for ZX Spectrum Next users:

1. **SD Card Utility** — mounts HDF disk images and provides a file explorer for uploading/downloading content to them, then launching the CSpect or MAME emulator.
2. **NextSync** — implements the server side of Jari Komppa's NextSync protocol (TCP port 2048), allowing files to be pushed from the PC over Wi-Fi to a physical Spectrum Next machine. The app also implements a backwards-compatible **Sync4** extension that adds the reverse direction (`.sync -send <file|dir>` on the Next pushes files/directories *back* to the app); legacy **Sync3** dots keep working unchanged (PC → Next only). The `.dot` client source lives under `nextsync/sync/`.

Additional tabs provide online browsing of ZX Spectrum software via three third-party APIs: **GetIt** (`zxnext.uk`), **ZXDB** (`api.zxinfo.dk/v3`), and **zxArt** (`zxart.ee/api`).

## Running the application

```
python zx-next-unite.py
```

Requires Python 3.10+ (the documented floor — CI runs the test suite on both
3.10 and 3.13; the release binaries bundle 3.13) and PySide6:

```
python -m pip install pyside6
```

Or install everything (including the optional extras `pygame-ce`, `itch-dl`,
`Send2Trash` and `flask` — Send2Trash powers the "Send deleted files to the
Recycle Bin" Settings toggle, Flask the NextSync HTTP bridge; each toggle is
greyed out until its package is installed) with:

```
python -m pip install -r REQUIREMENTS.txt
```

No build step is needed for development. The test suite lives in `tests/` — run it all with `python tests/run_all.py` (plain scripts, no pytest; see `tests/README.md`).

Linting: `ruff check .` must stay green (config in `pyproject.toml` — deliberately permissive so the legacy monolith isn't churned; don't add ignores casually, and don't `ruff format` legacy files wholesale — format only new modules).

For quickly eyeballing the retro "Alien Floyd" Sir Clive promenade animations
(the ones drawn in `zxnu_pygame.py`), pass `--anim` to force one to play first
and switch the mode on without touching Settings — e.g.
`python zx-next-unite.py --anim aliens`. Choices: `walk`, `c5`, `ufo`, `aliens`
(the last spawns Clive's saucer plus the attacking green alien squadron).
Requires pygame.

`python zx-next-unite.py -start-remote-explorer-listener` opens the NextSync
tab's Remote Explorer view and starts its `.sync5 -listen` server right at
startup using the saved sync root (this run only — the saved Settings are not
modified; without a saved sync root the usual "pick a sync root first"
advisory is logged instead).

## Packaging (optional)

Releases are built by CI: pushing a `v*` tag runs `.github/workflows/release.yml`,
which gates on the test suite (and on the tag matching `ZX_NEXT_UNITE_VERSION`),
builds the three platform packages with PyInstaller and attaches them — plus
`nextsync/sync/server/dot/syncdev` as the `sync5` asset — to a **draft** GitHub
release for review and manual publish. The release job also gates on the
checked-in `syncdev`'s embedded banner equalling `ZX_NEXT_UNITE_DOTN_VERSION`
(CI byte-copies that binary, never rebuilds it — a forgotten `build_dotn.ps1`
run would otherwise ship a stale dot the in-app .sync5 updater then pushes in
a perpetual re-offer loop). The `sync5` asset name stays **extensionless by
contract**: a platform-suffixed rename (`sync5-linux.tar.gz`) would suddenly
match `select_zxnu_release_asset`'s Linux branch and the app self-updater
would unpack a Z80 binary as the app (`tests/test_api_parsers.py` pins this).
The in-app updater instead fetches it by exact name from the release tagged
`v{ZX_NEXT_UNITE_VERSION}` — its own tag, never `latest` — via
`_resolve_sync5_update_binary` in `zxnu_emulator_ops.py` (source-checkout
`syncdev` first, cached download second, banner + size verified everywhere). The asset names are a contract with the
in-app self-updater (`select_zxnu_release_asset` in `zxnu_config.py`):
`zx-next-unite-<tag>.exe` must stay the release's only `.exe`; the Linux
tar.gz / macOS zip each hold one version-stamped runnable that
`extract_zxnu_update_archive` unpacks next to the running app.

PyPI: `.github/workflows/publish.yml` builds the sdist + wheel and uploads
them via PyPI Trusted Publishing (OIDC, environment `pypi`, no stored token)
whenever a GitHub release is **published** — so `pipx install zx-next-unite`
tracks the releases. The `version` in `pyproject.toml` must match
`ZX_NEXT_UNITE_VERSION` and the tag (the release workflow gates on both).
Entry points: `zx-next-unite` (gui-script → `zxnu_app:main`) and `nextsync5`
(console script → `nextsync5:main`).

Flatpak (Linux): everything lives under `flatpak/` (manifest on the KDE 6.8
runtime + PySide BaseApp, desktop entry, AppStream metainfo, pixel-art icons
— dots, not the Sinclair rainbow) with the build/Flathub-submission steps in
`flatpak/README.md`. `.github/workflows/flatpak.yml` builds the
`zx-next-unite.flatpak` bundle as a workflow artifact (workflow_dispatch,
pushes touching `flatpak/**`, and `v*` tag creation — tag runs also attach
the bundle to the tag's draft release as
`zx-next-unite-<tag>-linux-x86_64.flatpak`, retrying while `release.yml`
creates the draft and degrading to a warning, never a failure) —
deliberately separate from `release.yml` so the self-updater's
asset contract stays untouched (a `.flatpak` asset can never match
`select_zxnu_release_asset`, which requires `.tar.gz`+`linux`). Inside the
sandbox `ZX_NEXT_UNITE_MODE=installed` is set via finish-args and the
self-updater steps aside when `FLATPAK_ID` is present (updates come from the
Flatpak remote).

Create a standalone executable manually with PyInstaller:

```
pip install pyinstaller
pyinstaller --clean --onefile --windowed --noupx zx-next-unite.py
```

If the optional itch.io feature is in use, also fully bundle `itch-dl` and its
dependencies so installs work in the frozen exe (its submodules are not pulled
in by a bare `import itch_dl`, and the in-process installer in `zxnu_itchio.py`
imports `itch_dl.config` / `handlers` / `downloader` / `keys` / `api`):

```
pyinstaller --clean --onefile --windowed --noupx --collect-all itch_dl --collect-all bs4 zx-next-unite.py
```

Note: in a frozen build `sys.executable` is the GUI exe, not a Python
interpreter, so itch-dl must be driven in-process — never via
`[sys.executable, "-m", "itch_dl", …]`, which would just relaunch the app. See
`_install_in_process` in `zxnu_itchio.py`.

## Regenerating embedded Qt resources

Background images are compiled into `rc_backgrounds.py` from `rc_backgrounds.qrc`. Regenerate after adding/removing image assets:

```
pyside6-rcc rc_backgrounds.qrc -o rc_backgrounds.py
```

## Source file map

| File | Role |
|---|---|
| `zxnu_main.py` | The app monolith (formerly `zx-next-unite.py`, renamed so it is importable): contains the single `MainWindow(QMainWindow)` class and most tab/pane UI logic (shrinking via the strangler extraction below). Startup runs at module level — importing it launches the app |
| `zx-next-unite.py` | Thin launcher shim (`import zxnu_main`) kept for the documented `python zx-next-unite.py` workflow and as the PyInstaller build target |
| `_ImagePathCombo` (in `zxnu_main.py`) | The SD Card tab's image-path box. Beyond the Windows "phantom activation" guard it owns the history-REMOVAL affordances (9.6.0): a '✕' button beside it, Delete on the highlighted dropdown row, and a right-click menu on the box (text area AND arrow) or on a dropdown row, all funnelled through `removeIndexRequested`/`clearHistoryRequested` into one persist-and-unload closure in `MainWindow.__init__`. Its docstring carries the three Qt rules that cost a rewrite — `view.keyPressEvent = …` is DEAD on a C++-created popup view (PySide6 dispatches virtuals to Python attributes only for Python-instantiated objects: use an event filter), `QComboBoxPrivateContainer` selects a row on ANY button release so a right-click LOADED the image (swallow the right button and drive the menu from the press), and `customContextMenuRequested` on the popup fires after the popup has closed on Windows |
| `zxnu_app.py` | PyPI entry point: `main()` imports `zxnu_main` (side-effect launch). `[project.gui-scripts]` in `pyproject.toml` maps the `zx-next-unite` command to it; `nextsync5` ships as a console script alongside |
| `zxnu_sdcard_explorer.py` | `SdCardExplorerPane`: the SD Card tab's dual local ⇄ disk-image explorer (widgets + navigation/model layer, incl. the lazy `hdfmonkey ls` tree). First strangler extraction from `MainWindow.__init__`: the operation layer (transfers, deletes, context menus, DnD, load pipeline) stays in `zxnu_main.py`, reaching the pane through a documented `hooks` protocol; MainWindow keeps aliases under the historical attribute names (`self.treeview`, `self.image_treeview`, …) plus one-line delegating wrappers, so existing code and the test suite work unchanged. Also the single source of the `IMG_*_ROLE` item-data constants, and (9.5.30) carries the vertical **emulator strip** down the local pane's left edge — the NextSync Remote Explorer's strip mirrored here, sharing its `EmulatorTab` widget and reading the same `zxnu_workers.emulator_launch_entries(host)` list, so the two strips and the SD Card Launch buttons can never disagree. Detection is not a Qt signal, so every place that learns an emulator appeared/vanished calls `host._refresh_emulator_strips()` (both panes, guarded), plus a `showEvent` safety net. 9.6.0: a strip tab right-click opens the shared `emulator_color_menu` and the tab wears the picked colour, read from the host's one map (see `zxnu_emulator_ops`) |
| `zxnu_config.py` | Constants, `SETTING_*` keys, API base URLs, UI string tables, color defaults, and pure helpers (`resource_path`, `qcolor_to_hex`, etc.) |
| `zxnu_api.py` | Online catalogue API layer (strangler extraction #2): shared HTTP retry helpers plus the GetIt / ZXDB / zxArt fetchers, response parsers, website-URL builders and thread-safe zxArt name caches. Pure Python — no Qt — unit-tested by `tests/test_api_parsers.py`; star-imported by `zxnu_main.py` so historical names keep working |
| `zxnu_workers.py` | Background threading primitives: `WorkerSignals`, `NextSyncSignals`, `HdfTaskSignals`, `HdfTaskWorker`, `HdfProgressDialog`, `DotDotFirstProxyModel`, plus `CompactButton` (the explorers' shared Up/Refresh/+ Drive button — caps its width from its own label's metrics instead of a hard `setMaximumWidth`, which truncated the longer translations; both `zxnu_remote_explorer` and `zxnu_sdcard_explorer` build their navigation rows from it, and `tests/test_remote_explorer_widget.py` tripwires both against a relapse to a plain `QPushButton`); the NextSync `-listen` worker (`run_remote_listen_server` + `RemoteExplorerSignals`) behind the Remote Explorer; the `EmulatorLaunch` namedtuple + `as_emulator_launch` coercion that `emulator_launch_entries` hands both strips (9.6.2 — it carries the blocked reason alongside the label and launcher, and still accepts the historical bare `(name, launch)` 2-tuple); and (strangler extraction #3) the classic Sync3/Sync4 server loop `run_classic_sync_server` (tested by `tests/test_classic_sync.py`) plus the hdfmonkey transfer worker bodies (`_run_get_task` / `_run_put_task` / `_run_delete_task`, scan helpers, sync framing + `getFileList`/syncpoint helpers) — all UI-free, driven by injected callables. Also the remote **self-update macro** — `("update_dot", local, dir, ver[, base, brand, marked_exit])` in `_re_session`, updating the .sync5 dot (the defaults) **or a ZX Next Remote .nex** (base = its file name, brand "ZXNextRemote", marked_exit=True → the success quit is `Q`+exit-mark: the .nex saves settings and soft-resets into NextZXOS; ZXNR 1.0.3+ answers `'U'`, and its default-on OS protection over `apps/`/`dot/`/`sys/` is named in refusals, staging put included) — runs stage-put → read-back byte-compare (wire checksums are in-flight only) → `'U'` release (dot v5.9+ closes its own file handle; renames on the open file fail, hardware-measured — and it comes BEFORE the `.bak` delete so a pre-5.9 dot fails with nothing deleted) → rm `sync5.bak` (load-bearing: NextZXOS refuses rename-onto-existing; `'X'` is path-based, legal after `'U'`) → two renames → targeted quit, as `local_cmds` continuation steps (rmtree's pattern — nothing interleaves; after `'U'` only path-based ops V/X/Q, or the freed handle number could be reused and closed by exit tidy-up — so once released, every FAILURE path also ends the session, and a lost rename reply is reported as unknown/mid-swap, never "nothing changed"). Outcome fires the `dot_update` signal exactly once — session death included, via a finally-block emit with the `.bak` recovery wording when the swap had started; tested by `tests/test_remote_listen.py` |
| `zxnu_remote_explorer.py` | `RemoteExplorerWidget`: the dual-pane local ⇄ Next file manager of the NextSync tab (drives the `-listen` worker via a command queue; covered headlessly by `tests/test_remote_listen.py` for the worker side). Dragging OUT of the Next pane (to the OS file manager or the other panes) starts a background staging download and serves the file URLs LAZILY at drop time — `_StagedDragMime` + `_begin_drag_stage`; the class docstrings carry the hard-won Windows OLE rules (never block the gesture, hover-probe vs drop discrimination, aged-out stage folders). 9.6.0 adds three things here: the local pane owns its own **drive combo** in its nav row (`local_drives=`, between Refresh and the Search label — it used to be the classic tab's combo left behind on a full-width row above the whole view, which cost a row of height and pushed the panes out of line); the **session tabs carry the session id** (`"<name-or-addr> #<sid>"`, the combo's own composition — two `.sync5 -L` sessions of one named machine were two identical tabs), full combo label in the tooltip; and `EmulatorTab` takes `tint`/`on_menu` for the **per-emulator colour** (module-level `emulator_color_menu` is THE editor, shared with `zxnu_sdcard_explorer` and the SD Card tab's Launch buttons). Also the **remote .sync5 update UI**: a per-sid ident store (`_peer_idents`, filed under the driven sid — the single display tuple stays) and a session-tab right-click entry that offers the update only to an OLDER `sync` ident (int-tuple compare; unknown/unparseable idents get a warned manual "Push new .sync5…", `httpbridge`/`n2n` never — ZXNR updates via itch.io), resolves the binary via the `sync5_update_source` hook, confirms with an editable target dir (default `c:/dot`), and enqueues `("update_dot", …)` on the TARGET session's own queue. The **ZXNR flavor mirrors every shape** for `httpbridge`/`n2n` idents (floor 1.0.3): the `zxnr_update_source` hook resolves `zxnextremote-<flavor>.nex` out of the newest itch.io extract folder (disk-only, cached per label repaint, cache cleared when an itch.io install lands), the confirm asks for the FULL remote .nex path (persisted in `SETTING_ZXNR_UPDATE_PATH` — pre-filled only when its file name matches the session's flavor, or the wrong machine could get the right build swapped over the WRONG file), and the enqueue carries base/brand/marked |
| `zxnu_http_bridge.py` | NextSync HTTP bridge: reusable Flask web server (stdlib-only import; Flask optional, loaded on start — `flask_available()` gates the UI) republishing a `-listen` session as HTTP routes for the Next's `.http` dot command. Used by the app (Settings toggle + port and max-connections boxes, `SETTING_NEXTSYNC_HTTP_BRIDGE`/`SETTING_NEXTSYNC_HTTP_PORT`/`SETTING_NEXTSYNC_HTTP_CONNECTION_LIMIT`, greyed without Flask) and `nextsync5.py -w`/`-http[=port]` (+`-flask-connection-limit:<n>`, default 1); docs + call samples in `nextsync/sync/server/HTTP_BRIDGE.md`, e2e test in `tests/test_http_bridge.py`. Optional bearer-token protection: `NextSyncHttpBridge(auth_token=…)` installs a `before_request` guard (constant-time `hmac.compare_digest` on the `ZXNEXTUNITE-BRIDGE-TOKEN` header, else HTTP 401); the app exposes it via a Settings "Require bearer token" checkbox + token field + Generate button (`SETTING_NEXTSYNC_HTTP_TOKEN_ENABLED`/`SETTING_NEXTSYNC_HTTP_TOKEN`, off by default — a 64-char token minted by `generate_bridge_token()` on first enable) |
| `espemu.py` | Clean-room ESP-AT emulator (stdlib-only, no Qt): `EspAtEngine` (the pure AT state machine, gateway-injected) + `EspAtServer` (selectors loop on a daemon thread; MAME connects to it via `-rs232_esp null_modem -bitb socket.127.0.0.1:<port>`). ONE server, MANY emulators, A THREAD EACH: the accept loop forks every MAME that connects into its own `_EspSession` (engine + links + CIPSERVER listener + serial queue + backpressure band + **its own selector and loop thread**), so several emulated Nexts — one per disk image — are on the network at once and cannot see a byte of each other's AT state. The thread each is load-bearing: a shared loop was tried first and review measured two cross-tenant failures — a malformed `AT+CIPSTART` host raises `UnicodeEncodeError` out of getaddrinfo (NOT an `OSError`), which tore the loop down and killed every session, and the two bounded blocking gateway calls (`CONNECT_TIMEOUT_S`, `SEND_TIMEOUT_S`) stalled bystander emulators up to 9.7 s, past the ~3 s at which the Next resets its module. Per-session state needs no lock (single-threaded within a session); the two genuinely shared tables — the session list and `_net_claims` — each have one, and `_handle` is wrapped per event so a bad event costs an event. The one genuinely shared resource is the PC port space: when two guests ask CIPSERVER for the same port, `_net_claims` + a bind ladder move the later one up to `CIPSERVER_PORT_SEARCH` ports and LOG which port to connect to. Driven by the Settings "RS232 ESP Emulation (Mame)" toggle (`SETTING_MAME_RS232_ESP`/`_PORT`/`_VERBOSE`): Launch Mame REUSES the running worker (freshness is per session now, and restarting would cut the wire of a MAME already running), starting one only when none is up (bind is synchronous, so MAME cannot race it), splices the two MAME options in with the server's ACTUAL port (stripping hand-typed copies while the toggle is on), and reference counts it — `host._esp_emu_users`, one per launched MAME, the last one out calls `_stop_esp_emulation`. A port change while a MAME still holds the worker is logged and deferred; a verbose change applies live via `set_verbose`. Verbose tracing is rate-limited (`_TraceGate`) and marshalled to the SD Card log console via `EspEmuSignals` (zxnu_workers). Transfers through it REQUIRE the Next side on slow pacing (`.sync5 -s` / ZXNR UART "Slow"). Inspired by Janko Stamenović's jesperl (credited in Help/notices; clean reimplementation, MIT). Tested by `tests/test_esp_emu.py` |
| `nextsync5.py` | Standalone Sync4 NextSync command-line server (moved to the repo root so it sits next to `zxnu_http_bridge.py`); `-listen` console, `-w`/`-http[=port]` web bridge, `-v` also traces every HTTP request/response. The console's `release` verb sends the dot's `'U'` op, and `update <file> [dir] [force]` runs the whole remote self-update macro for EITHER flavor (same steps as zxnu_workers' `update_dot`, on the `upd_steps` list popped ahead of the queue), gated on the cached `version` ident: a `sync` dot uses the banner + older-than gate and the `c:/dot` default; an `httpbridge`/`n2n` listener needs ZXNR 1.0.3+, reads the staged version from a `zxnextremote-X.Y.Z` folder name, REQUIRES an explicit `[dir]`, and finishes with the marked quit — deliberately NOT exposed over the HTTP bridge. Protocol tests in `tests/test_listen.py` |
| `zxnu_media.py` | ZX Spectrum `SCREEN$` decoder (`ZxSpectrumScreen`), placeholder-pixmap rendering, file-format tag helpers, and the shared pixmap cache |
| `zxnu_gallery.py` | Reusable gallery widgets: `GalleryCell`, the scrollable grid view (`GalleryView`), the `AnimatedBackground` widget, and `_DblClickFilter` — plus `BackgroundWidget`, the window's whole client area: it paints the user's Background colour as a solid ground and composites the cycling artwork over it (a plain QWidget paints no background of its own, which is why the ground used to be whatever the platform filled behind the window) (the double-click event filter shared by all three gallery panes' preview labels) |
| `zxnu_getit_pane.py` | `build_getit_pane(host, …)`: the GetIt (`zxnext.uk`) gallery tab's ~1.5k-line UI-construction blob — the first of the three gallery-pane builder extractions and the template the ZXDB/zxArt twins follow (see `zxnu_zxdb_pane.py` for the seam details). Same injected-hooks + `host.getit_*` attribute pattern; outputs re-bound to `__init__` are `getit_run_search`/`getit_on_latest`/`getit_on_random`/`_getit_open_gallery_viewer` |
| `zxnu_zxdb_pane.py` | `build_zxdb_pane(host, …)`: the ZXDB (ZXInfo API v3) gallery tab's ~3k-line UI-construction blob (widgets + navigation + search/detail/download/context-menu closures), strangled out of `MainWindow.__init__`. Unlike the SD-Card pane this is a *builder function*, not a class: every closure that assigned `self.zxdb_*` now writes `host.zxdb_*`, so MainWindow keeps all its historical `zxdb_*` attributes automatically. The `__init__`-locals it reads (tab spinners, cross-search dispatch, `execute_hdf_monkey`, `save_configuration_file`, shared retro/gallery UI helpers, the module-global disk-image content) are injected as keyword-only params — passed by identity when defined before the call, as forwarding lambdas when defined later in `__init__`. The four closures the rest of `__init__` still calls (`zxdb_run_search`/`zxdb_on_latest`/`zxdb_on_random`/`_zxdb_open_gallery_viewer`) are written to `host` and re-bound to bare `__init__` locals right after the call |
| `zxnu_zxart_pane.py` | `build_zxart_pane(host, …)`: the zxArt (zxart.ee) gallery tab — the twin of `zxnu_zxdb_pane.py`, built with the identical seam (see that row). Its extra dependencies are the zxArt name-resolution/scrape helpers and the `_zxart_lang`/`_zxart_set_language`/`_zxart_tr` i18n helpers (all from `zxnu_config`/`zxnu_api`). Both pane modules are guarded by `tests/test_pane_imports.py`, a free-variable tripwire asserting the builder resolves every name it uses (params + imports + star exports), which catches the star-import-skips-underscores trap |
| `zxnu_unite_pane.py` | `build_unite_pane(host, …)` + `build_unite_ops(host, …)`: the Unite! (AllInOne) tab, extracted as two builders sharing the gallery panes' seam. The first is the widget layer (search row, table/gallery stack, preview panel, tab insertion + colour-cycle timer), called at the tab-construction spot; the second is the operation layer (aggregation of the last GetIt/ZXDB/zxArt/itch.io results, fan-out Search/Latest/Random, merged autocomplete, the view-mode apply helper and the optional pygame "Retro" mode), called right after the itch.io tab block at the ops blob's historical position — the view-mode/pygame sections were folded in from further down (safe: they only define closures/connect signals; their one construction-time call touches only widgets built by the first builder). Inside the three pygame closures the original local variable `host` was renamed `pg_host` so it can't shadow the builder's `host` param. Guarded by `tests/test_pane_imports.py` |
| `zxnu_settings_pane.py` | `build_settings_pane(host, …)`: the whole Settings-tab construction blob (~1.6k lines — every settings row widget plus its inline persistence-handler closures: self-update/theme/colors/gallery/NextSync HTTP bridge + bearer token/MAME/CSpect/Alien Floyd's/itch.io/conflict policy/sort mode…). Writes every historical `self.settings_*` attribute to `host` (load_configuration_file restores checkbox state through them) and hands back `host.settings_scroll` for the monolith's one remaining `addTab` line. Row placement goes through the `SETTINGS_TAB_ROWS` name registrar — see "Settings tab rows" under Key architecture patterns. 9.6.1 adds the **Background** colour (`SETTING_COLOR_BACKGROUND`, top of the colours section) and a **Reset theme** button closing it. Background is the set's only non-foreground: `_apply_background_color` pushes it to the window fill (`BackgroundWidget.set_bg_color`, painted UNDER the cycling artwork) and to the explorer viewports (`views_background_qss` in `zxnu_config`, at `NEXT_VIEWS_BG_ALPHA` so the animation still shows through, which also pairs a `contrasting_text_hex` default item colour with the ground — the two plain `QFileSystemModel` local explorers (SD Card, NextSync classic) set no foreground brush, so they took the OS palette's text colour and a light Windows theme drew them BLACK: fine on the stock white viewport, invisible the moment the ground went dark). It is a `_theme_palette` entry whose light and dark columns share ONE dark value — the item colours are tuned for a dark ground and the Desktop Theme only ever claimed to choose FONT colours, so a Windows classic/light theme no longer drags the panes to stock white under green file names (high contrast is the exception: black fonts, white ground). Two ordering rules were paid for here: the app-wide stylesheet is composed at the END of `_apply_desktop_theme_colors`, never at the top (the ground is read from `img_color_background`, which the palette loop has to recompute first) and in BOTH of its paths (the Custom branch returns before the tail hooks); and `__init__` applies it again after `load_configuration_file`, because on a first run there is no cfg and the whole restore body — theme re-apply included — is skipped |
| `zxnu_nextsync_pane.py` | `build_nextsync_pane(host, …)`: the NextSync tab's widget + wiring layer (classic local explorer with drive combo/filter/tree, sync-root row, log window + retro toggle, the Remote Explorer ⇄ Classic experience selector, the `RemoteExplorerWidget` + `-listen` server-control block, HTTP-bridge plumbing, sync-mode radio group, start/cancel/progress row). The `nextsync_*` operation closures stay in the monolith and are injected as keyword-only params; `nextsync_container` and `_re_try_send_folder` are handed back as host attributes and re-bound to bare locals at the call site. `available_drives` must stay a param (not module state): the non-Windows path appends to the `__init__` list without ever rebinding it first. 9.6.0: the Remote Explorer view hides the classic drive/filter bar with `nextsync_form.setRowVisible(host._nextsync_classic_bar_row, False)` — **not** three `setVisible(False)` calls, which leave a zero-height row that still collects the form's vertical spacing; that spacing plus two stacked 9 px top margins (now zeroed on `nextsync_form` and `nextsync_container_log_and_sync_buttons`) was 30 px of dead band under the mode tabs. `setRowVisible(…, True)` shows EVERY widget in the row, so the drive combo's platform gate is re-applied after it. Two more layout rules learned there: `horizontal10` ends in `addStretch(1)` (the SD Card tab's twin row always had one — without a stretchable item Qt hands the slack to the widgets, which ballooned the drive combo and pushed the filter box a screen away from its "Search:" label), and the Classic explorer column's layout pins `setContentsMargins(0,0,0,0)` to match the log container beside it — pinned rather than adjusted, because a QLayout with no parent widget yet reports `(0,0,0,0)` and the style's 9 px only arrives on `setLayout()` |
| `zxnu_itchio_pane.py` | `build_itchio_pane(host, …)`: the whole optional itch.io tab block (state init, auth row, collections combo, search, table/gallery stack, item viewer with install/uninstall/send-to-SD-card actions, cross-search registration, conditional tab insertion — only built when `itch-dl` is importable; the `host._itchio_*` state attrs are initialised unconditionally so the rest of the app can `getattr()` them). Same seam as the gallery panes; the module-global `right_disk_image_explorer_content` is read via the `_right_disk_content` getter hook |
| `zxnu_favorites_pane.py` | The Favorites tab + per-pane Classic ↔ Retro routing, as THREE builders because the original block is interleaved with the pane-builder calls that stay in the monolith — each called at its chunk's historical `__init__` position: `build_favorites_helpers` (cross-pane record/toggle/save/navigate helpers; runs before the pane builders that consume `host._fav_is`/`_fav_toggle`, so the three `*_run_search` closures they bind later arrive as forwarding lambdas), `build_favorites_pane` (tab widget layer; also exposes the `_fav_*` getters the Unite! builders consume, re-bound to bare locals at the call site) and `build_favorites_ops` (`_fav_repopulate` + view-mode apply + the `_pane_*` item-viewer routing; its closures' local PygameSurfaceWidget variable `host` was renamed `pg_host`) |
| `zxnu_emulator_ops.py` | Pure operation layer, no widgets — two builders: `build_emulator_ops` (CSpect/MAME settings setters, `launch_cspect`/`launch_mame`, the MAME install/update chain, the zx-next-unite self-update chain, the `.sync` dotN version advisory, the CSpect update chain, `_wire_viewer_emulators`, and — 9.6.0 — the **per-emulator colour store**: `host.emulator_color_for` / `host.set_emulator_color` / `host._apply_emulator_button_colors` over one `SETTING_EMULATOR_COLORS` JSON map keyed by `emulator_color_key(label)`, so "Mame", "Mame (flatpak)" and "🕹  Launch Mame" all resolve to one entry and the two strips + the two Launch buttons can never wear different colours). 9.6.2 adds the **disk-image launch gate**: `_image_busy_reason` / `_probe_image_write_access` / `_reprobe_and_regate` / `_refresh_emulator_launchability` over a `host._image_write_state` cache plus a `host._images_held_by_us` reference count, all funnelled into `_emulator_launch_blocker` — see "Disk-image launch gate" under Key architecture patterns. Also `build_hdfmonkey_install_ops` (the jjjs zip-inside-a-zip hdfmonkey download/install/discovery chain + missing-hdfmonkey prompt wiring). Both blocks are definition-only so the builders are safe at their historical positions; the closures `__init__` wires to widgets by bare name are exposed on `host` and re-bound at the call sites |
| `zxnu_nextsync_ops.py` | NextSync tab operation layer (the widgets live in `zxnu_nextsync_pane.py`) — three builders called at their historical `__init__` positions because the blocks interleave with the SD-card ops: `build_nextsync_server_start` (prepare-debounce timer, perform-checks/refresh, `nextsync_start_server` — warnings/do_server_job/`_re_try_send_folder` arrive as forwarding lambdas resolved at server-start time), `build_nextsync_explorer_ops` (classic-explorer context menu, rename/copy/paste/import/delete, sync-root row, syncignore/syncpoint buttons, sync-mode persistence) and `build_nextsync_server_job` (warnings, IP info, cancel, Sync4 conflict-prompt marshalling, `nextsync_do_server_job`). The start-server closure is exposed as `host._nextsync_start_server_fn` — the bare `nextsync_start_server` attribute is the Start button widget |
| `zxnu_sdcard_ops.py` | SD-card tab operation layer (the widgets live in `zxnu_sdcard_explorer.py`) — four builders: `build_sdcard_utils` (load pipeline incl. `load_image`/`select_image`/`download_nextzxos_image`, the three log windows, new-folder chain, usage gauge, `execute_hdf_monkey`), `build_image_edit_ops` (image delete + in-image rename), `build_local_explorer_ops` (SD local-pane context menu, watcher-suspended delete, zip/unzip, import, plus the create-directory/rename helpers shared with the NextSync classic explorer) and `build_transfer_clipboard_ops` (image ⇄ disk transfers, the shared cross-explorer clipboard, remote zip, image context menu, `SdCardExplorerPane` delegate wrappers). The module globals `right_disk_image_explorer_content`/`right_disk_image_selected_files` STAY in `zxnu_main` — moved code reads via the `_right_disk_content` getter and writes via the `_set_right_disk_content`/`_set_right_disk_selected` setter closures injected at the call sites |
| `zxnu_tab_ops.py` | `build_tab_ops(host, …)`: cross-tab operation helpers — search-autocomplete enable/apply, the Unite! multi-search gate + the three cross-search fan-out helpers, the tab-title badge/spinner machinery (their QTimers built in-builder at the historical position), the autocomplete-arrow animation, and `on_tab_changed` (the `currentChanged` handler driving per-tab side effects) |
| `zxnu_retro_ui.py` | Four small builders: `build_main_retro_log` / `build_help_retro_log` (the SD-card and Help tabs' "🎮 Retro" pygame-log toggles), `build_sidebar_anim` (sidebar sync-activity getter + Unite!/Remote-Explorer tab-title colour-cycle timers) and `build_content_disclaimer` (the one-time content-disclaimer dialog) |
| `zxnu_config_io.py` | `build_config_io(host, …)`: `load_configuration_file` (the whole `hdfg.cfg` restore pipeline that pushes every saved setting back into the constructed widget tree, incl. the itch.io auto-connect and the `-start-remote-explorer-listener` startup hook) + `save_configuration_file` (the `_initialising`-guarded UTF-8 writer). `wid_inner` and the module-global `_ZXNU_START_RE_LISTENER` flag are read via getter hooks; both closures are re-bound to bare `__init__` locals at the call site because dozens of later closures and builder calls capture them |
| `zxnu_i18n.py` | UI internationalisation (ES/PT/PL/RU/CS/FR): per-language catalogs keyed by the exact English source string plus `translate_widget_tree`, a walk over the constructed widget tree that swaps label/button/checkbox/radio/groupbox/placeholder/tooltip texts in place (no tr() wrapping, no .ts/.qm toolchain). Each widget's source text is cached so switching languages — including back to English — is lossless, and app-rewritten texts are adopted, not clobbered. Deliberately never touches QTabWidget titles or QComboBox item texts (dispatch keys / compared option values) — the sole exception is opt-in: a combo passed to `mark_combo_items_translatable()` DOES get its items translated, which is only safe once nothing reads its `currentText()` as a key. The SD Card tab's CSpect/MAME option combos are marked, having first been converted to read their selection by index (`emulator_option_argument` in `zxnu_config.py`, replacing the old label lookup) so the launch command lines stay identical in every language. Driven by Settings → "Application language:" (`SETTING_UI_LANGUAGE`, live re-translate) and applied at the end of `MainWindow.__init__`; on the very first run (blank `ui_language`) the OS language is adopted when it is one of the shipped six (`system_ui_language`, overridable via the `ZX_NEXT_UNITE_UI_LANGUAGE` env var), persisted once, and announced with a 15 s bottom-left toast in that language. **Toasts are fully translated**: `translate_widget_tree` records the active language (`set_current_ui_language`), `ui_tr_now(text)` translates runtime strings against it, `MainWindow._show_toast` is the chokepoint for static toast titles/bodies, and composed/dynamic toast bodies are built from `ui_tr_now("… {placeholder} …").format(…)` TEMPLATES at their call sites (the catalog keys ARE the templates — see `_TOAST_CATALOGS`). `tests/test_i18n.py` carries a tripwire (AST scan of `_show_toast`/`_on_toast`/`_show_sd_notification`/`ui_tr_now` literals) failing when a new toast string is missing from any language. **Context menus and file-op dialogs are translated the same way**: every menu action label (menus are rebuilt per right-click, so `ui_tr_now` picks the live language) and the delete/rename/new-folder/sync-root/add-drive/zip confirm + failure dialogs go through `ui_tr_now` at their call sites — the tripwire also scans `zxnu_sdcard_ops`/`zxnu_nextsync_ops`/`zxnu_favorites_pane`/`zxnu_pygame`, so a new `ui_tr_now` literal missing from any catalog fails the suite. **Widgets built AFTER startup translate themselves**: the gallery item viewers open on demand, long after the startup walk, so `GalleryItemViewer.__init__` ends with `translate_widget_tree(self, current_ui_language())` on its own finished subtree (its catalog strings existed for ages but nothing ever applied them), and `PygameItemViewer` — which draws its own buttons — runs each label through `ui_tr_now` at construction. Labels a caller overrides later (`set_emulator_actions`, `set_open_web_url`, …) go through `ui_tr_now` at those call sites. **The NextSync console is translated too, but only its user-facing half**: session status, the host/IP banner, transfer progress and the sync-root guidance go through `ui_tr_now` at their call sites in `zxnu_workers`/`zxnu_nextsync_ops`/`zxnu_nextsync_pane` (composed lines use `ui_tr_now("… {field} …").format(…)` templates). Protocol diagnostics — packet/checksum/sequence/version lines — stay English ON PURPOSE so a pasted log still matches the docs and the `.dot` source, and commands the user must type on the Next (`.sync5 -listen`, `.sync5 -send …`) are interpolated as `{command}` rather than sitting inside a translatable string. `tests/test_i18n.py` enforces all three rules: every template keeps its `{placeholders}` and actually `.format()`s in all seven languages, every Next command survives translation verbatim, and no protocol diagnostic gets translated. **The Help ("?") tab is translated too**: `INIT_HELP` (zxnu_config) renders through `ui_tr_now` line by line and `_repopulate_help` (zxnu_main) rebuilds the whole list — including the retro console mirror — at construction and on every language switch (`_i18n_apply` calls it after the widget walk, since list CONTENT is invisible to the walk); the version banner uses the `"Welcome to zx-next-unite {version} help"` template. Structural lines stay English on purpose: blanks, dash underlines, URL-only lines, `API base URL :` rows and the proper-name section headers — and quoted UI-label references inside prose (e.g. 'Download and install HDF Monkey') stay English verbatim, matching the buttons themselves. `tests/test_i18n.py::test_help_tab` enforces full prose coverage in all six catalogs plus verbatim survival of URLs/commands/file names. Still deliberately English: the emulator/self-update advisory + itch.io picker dialogs. Tested by `tests/test_i18n.py` + offscreen phases 9–10 |
| `zxnu_wizard.py` | Wizzy, the animated onboarding wizard: a pure-Qt (no pygame needed) pixel-art assistant in the bottom-left corner. Sprite follows the Clive convention (string rows + palette, chunky QPainter cells — NO Sinclair rainbow stripes, dots only, trade-dress rule); gestures: idle bob/blink, look (head turn), wave, point, cast (the wand appears/sparkles/disappears), talk, and a walk-like-an-Egyptian profile promenade along the bottom edge (mirrored for leftward strolls). First run: introduces itself, offers the tab tour or its own off switch (`SETTING_WIZARD_ENABLED`/`SETTING_WIZARD_INTRO_SHOWN`, the `wizard` Settings row). Tour steps switch tabs (hidden tabs skipped) and async-fetch a "From the manual" teaser from the GitHub wiki page per tab; "Read the manual" opens the wiki. In-depth GUIDES (branching node graphs in `zxnu_wizard_content.py`, engine in `zxnu_wizard.py`): the first visit to a guided tab per session (SD Card, NextSync) offers "discovery tour or tell me more?"; guide bubbles carry a persistent Manual+GitHub links row, optional "Take me there" tab jumps and Linux-only notes (`_is_linux()` gate, patchable in tests). Built via `build_wizard(host, …)`; deferred `startup()` runs 2.2 s after `__init__` (config already restored). Tested by `tests/test_wizard.py` |
| `zxnu_wizard_content.py` | Pure data for the wizard: every dialogue line in all 7 UI languages (`TEXTS` key→lang→text, tripwired complete by `tests/test_wizard.py`), `JOKES`/`STORIES` per language, `TOUR_STEPS` (tab-title constant, dialogue key, wiki page) and the wiki URL templates. The per-tab deep-dive content deliberately lives on the GitHub wiki user manual (one page per tab), not here |
| `zxnu_itchio.py` | Optional itch.io integration: `itch-dl` detection, itch.io API access (collections/owned/search via the user's API key) and install-via-`itch-dl`. Drives the optional itch.io tab |
| `rc_backgrounds.py` | Auto-generated Qt resource module (do not edit by hand) |

## Key architecture patterns

**Configuration and per-user state** live under ONE data root, `ZXNU_DATA_ROOT` (resolved once at `zxnu_config` import): `hdfg.cfg` (a `key = value` file, INI-like), the rotating/crash logs, and the whole `downloads/` tree — which **all emulator/hdfmonkey detection is rooted in** (the itch.io CSpect walk under `downloads/itchio/`, `downloads/hdfmonkey/<platform>/`, `downloads/mame/`), so never anchor any of those anywhere else. Resolution order: `ZX_NEXT_UNITE_HOME` env override → frozen exe dir (portable, the historical behavior) → platform app-data dir when `ZX_NEXT_UNITE_MODE=installed` (set by the PyPI entry point `zxnu_app.main`; `%APPDATA%\zx-next-unite` / `~/Library/Application Support/zx-next-unite` / `$XDG_DATA_HOME|~/.local/share/zx-next-unite`, with one-time migration of strays a 9.1.9-era pipx install left next to the script) → the launched script's dir (source checkouts). `zxnu_main.py`'s early logging block mirrors this in `_zxnu_early_state_dir()` — keep the two in sync. The one deliberate argv-anchored exception: the self-updater downloads new binaries next to the RUNNING executable, not into the data root. Tested by `tests/test_data_root.py`. All setting key names are `SETTING_*` constants in `zxnu_config.py`. The `_initialising` guard on `MainWindow` prevents `save_configuration_file()` from firing while widgets are being constructed.

**Settings tab rows are placed by NAME, never by grid index.** The Settings tab is one `QGridLayout` whose visual order is defined solely by the module-level `SETTINGS_TAB_ROWS` tuple in `zxnu_settings_pane.py` — one name per row, top to bottom — and every widget is placed with `settings_grid_row("<name>")`. **To add a setting: insert its name in the tuple where it should appear, then place its widgets with that name** (label in column 0 + field in column 1, or one widget spanning `1, 2`); every row below moves down by itself. Never pass a literal row index to `grid_tab_Settings.addWidget/addLayout` — before the registrar, inserting one checkbox meant renumbering 30+ call sites by hand. A typo'd name raises `ValueError` at build time and a duplicate name fails the import; the two platform-exclusive MAME rows (native update-check checkbox / Flatpak options box) share one name on purpose. `tests/test_ui_offscreen.py` asserts widget positions through the same mapping (plus relative-order checks), so a call site that bypasses the registrar fails the suite.

**Threading** follows a consistent pattern: long-running work (hdfmonkey commands, network fetches, NextSync transfers) runs in a `QRunnable` submitted to `QThreadPool`. Results are marshalled back to the UI thread via `Signal`/`Slot`. The signal classes (`HdfTaskSignals`, `NextSyncSignals`, etc.) live in `zxnu_workers.py`.

**External tool dependencies** — `hdfmonkey` is invoked as a subprocess for all HDF image operations. When it is missing, the app can auto-download a pre-compiled build for the current platform (Windows/Linux/macOS) via the "Download and install HDF Monkey" button. The source is the jjjs release at `HDF_MONKEY_JJJS_URL` (`specnext.com` forum attachment `id=1159`): a zip-inside-a-zip whose inner archive is ZipCrypto-encrypted (password in `password.txt`, currently `jjjs`) and carries per-platform folders (`windows-64/`, `linux-musl/`, `macos-intel/`, `macos-mn/`). `extract_hdfmonkey_from_jjjs_zip` (in `zxnu_config.py`) unpacks just the current platform's binary into `downloads/hdfmonkey/<platform>/`, which `find_hdfmonkey_in_downloads` re-discovers on later launches. The recommended route remains a full CSpect install from the itch.io tab, which also bundles hdfmonkey (discovered by `find_hdfmonkey_near_cspect` / `find_emulators_in_downloads`). CSpect and MAME are detected via PATH and launched as subprocesses. **OpenAL (Windows sound for CSpect)**: after an itch.io CSpect install the detection toast runs `is_openal_installed()` (`zxnu_config.py` — deliberately privilege-free: `System32`/`SysWOW64` `OpenAL32.dll` file check plus a READ-ONLY Add/Remove Programs Uninstall-key scan) and, only when the runtime is missing, `_offer_openal_install` (`zxnu_emulator_ops.py`) offers the guided install: download `OPENAL_DOWNLOAD_URL` (oalinst.zip) into `downloads/openal/`, `extract_oalinst_from_zip`, launch `oalinst.exe` interactively — elevation comes from the installer's own UAC manifest, the app never runs elevated. Linux/macOS return True from the detector (system OpenAL — nothing to install). Tested by `tests/test_openal_install.py`. `resource_path()` in `zxnu_config.py` handles path resolution for both development (source tree) and PyInstaller-frozen (`sys._MEIPASS`) contexts.

**Disk-image launch gate (9.6.2)** — an emulator holds the `.img`/`.hdf` it was handed for its whole run, so a second one pointed at the same file dies mounting it (MAME can be worse: it falls back to mounting READ-ONLY, so writes are silently lost). Every affordance that boots the mounted image — the SD Card tab's two Launch buttons, both vertical emulator strips, the gallery item viewers' Launch buttons in Classic *and* Retro — asks the one `_emulator_launch_blocker(emulator)` in `zxnu_emulator_ops.py`, which now ends each branch with `_image_busy_reason`; the reason string doubles as the tooltip. **Never re-derive the launch rules at a call site** — that is how the viewers came to disagree with the SD Card tab.

The probe is `probe_image_write_access` in `zxnu_config.py`, and its share mode is load-bearing: it asks for **EXCLUSIVE** access (`CreateFileW` share mode `0`), *not* `open(path, "r+b")`. Measured against both emulators on Windows 11 — MAME opens `-hard1` with `FILE_SHARE_READ` (a plain write-open sees it), but **CSpect opens `-mmc` with full read/write sharing**, so a write-open succeeds while CSpect is running and the gate would never fire for it. `tests/test_image_lock.py` pins both holder shapes, so a "simplification" back to `r+b` fails the suite. `ERROR_SHARING_VIOLATION` (32) is BUSY; `ERROR_ACCESS_DENIED` (5) is DENIED and deliberately does **not** grey anything (a read-only image, or a path a Flatpak sandbox cannot see, is a different failure the user could not clear). Note Python's builtin `open()` collapses both to `PermissionError errno 13` with `winerror = None`, which is why the helper uses ctypes.

**Platform honesty:** the probe only answers on Windows. POSIX has no mandatory locking and neither emulator takes an advisory `flock`, so on Linux/macOS — native MAME, Flatpak MAME, CSpect under mono, all of macOS — it always returns OK. There the gate runs on `host._images_held_by_us` — `{key: [process, …]}`, the MAME `Popen` recorded in `launch_mame`. **Processes, not a reference count**: a count needs an exit handler to decrement it, and anything that misses one leaves the entry stuck or wrongly cleared — `poll()` is the authority instead, so a holder that has gone simply stops counting. (An earlier count plus a Windows "self-heal" could pop a LIVE reference during the ~2 s between MAME's `Popen` and MAME actually opening `-hard1`.) This survives both Flatpak modes (`flatpak run` and `flatpak-spawn --host` both keep the handle alive for the child's lifetime) but cannot see an emulator started outside the app. The launch-click guard (`_reprobe_and_regate`) must consult BOTH this record and the probe — the probe alone let a second MAME boot a held image on POSIX, and on any platform through a gallery item viewer whose Launch button was wired while the image was still free. CSpect needs no counterpart: `execute_shell_command` is a blocking `subprocess.run`, so a CSpect the app launched is never running while the user can click.

`_image_state_key` is the ONE key rule — `abspath` (which also collapses `.`/`..`) plus case-folding on Windows, with the empty-path check FIRST so `abspath("")` cannot key every blank path to the working directory. Producer and consumer must both go through it: the image box is free text and the launchers `abspath` what they hand the emulator, so a relative `next.img` recorded against an absolute lookup is a gate that silently never fires. Verdicts are **cached** (`host._image_write_state`, under that same key) because the gating helpers run from tooltip refreshes. Re-probe only at the points where the answer can have changed: the launch click itself, a successful `load_image`, re-picking the loaded image from the history dropdown, a tracked MAME exiting, and entering the SD Card tab while the last verdict was busy. `set_all_buttons_enabled()` must NOT blanket-enable the two Launch buttons — it calls `_update_cspect_controls()` / `_update_mame_controls()` instead, or every finished transfer silently un-greys a button whose image is still held. Those two re-gate the BUTTONS only, so any path that changes launchability must also reach the strips: use `_refresh_emulator_launchability()` (both, plus `_refresh_emulator_strips`), or a live button ends up sitting beside a greyed, inert strip tab. Autostart launches (running a downloaded `.nex` with no image) are never gated.

**Right-click is the way out of a greyed launch (9.6.2)** — the one shared `emulator_color_menu` (`zxnu_remote_explorer.py`) now opens with a disk-image section: `host.writable_image_choices()` probes every remembered image in the `imageinput` combo and offers the writable ones as "Select emulator image file: …", with the loaded one check-marked; picking one runs `host.select_emulator_image()` (set the box, then `load_image`), which re-probes and re-gates on its way through. Opening the menu also refreshes the cached verdicts, so it doubles as the manual "is it free yet?" gesture. **The Launch buttons bind it with an EVENT FILTER, not `setContextMenuPolicy(CustomContextMenu)`** — that policy lives inside `QWidget.event()`, and Qt drops context-menu events for a DISABLED widget before `event()` runs, so the signal never fires on a greyed-out button, which is exactly when the menu is needed. Event filters run first. Measured, not assumed, and tripwired in offscreen phase 12; the same trap `_ImagePathCombo` documents for the combo popup. The filter is installed on the button AND its parent (the two delivery routes differ) and returns True so only one menu opens. The strip tabs need none of this: they stay `enabled` and only swallow the LEFT click, so `contextMenuEvent` still reaches them.

**Gallery panes** (GetIt, ZXDB, zxArt, Unite!) share the `GalleryCell` widget and a common pagination + async thumbnail-fetch pattern. Feature flags in `zxnu_config.py` (`ZX_NEXT_UNITE_SHOW_ZXDB_PANE`, `ZX_NEXT_UNITE_ZXDB_ENABLE_DOWNLOAD_BUTTONS`, etc.) can hide entire panes or their download actions without touching the UI code.

**Optional itch.io tab** — built only when the optional `itch-dl` package is importable (`zxnu_itchio.itchdl_available()`). Authentication is a personal itch.io API key stored in `hdfg.cfg` (`SETTING_ITCHIO_API_KEY`); browsing of the user's collections/owned games/search uses the public itch.io API directly, while installing a selected item is delegated to `itch-dl` (downloaded to `downloads/itchio/`). The tab registers itself with the shared `_fav_fetchers` dispatch so its items also appear in the Unite! multi-search when a key is set. A Settings checkbox (`SETTING_SHOW_ITCHIO_TAB`, default on) shows/hides the tab the same way the Alien Floyd's tab toggle does.

**Logging** has two layers. An **always-on rotating diagnostic log** (`zx-next-unite.log` next to the exe, `%TEMP%` fallback; `RotatingFileHandler`, ~1 MB × 3) is configured by `_zxnu_configure_logging()` and captures all `logging.*` output plus uncaught exceptions — critical because in a `--windowed` build `sys.stderr` is `None`, so without it every `logging.error(...)` is thrown away. Prefer `logging.exception(...)` (UI stays quiet) over `except …: pass` for load-bearing failures; cosmetic best-effort guards (deleted-widget `RuntimeError`, tooltip/animation) can stay bare. **Crash logging** on top of that is opt-in (Settings → "Enable crash log file generation"): `_zxnu_open_crash_log()` wires `faulthandler` (and the excepthooks' extra file write) to `zx-next-unite-crash.log` for C-level crashes.
